<Modal @ref="Modal">
    <ModalContent Centered="true">
        <ModalHeader>
            <ModalTitle>@Title</ModalTitle>
            <CloseButton Clicked="AbortModal" />
        </ModalHeader>
        <ModalBody>
            @ChildContent
        </ModalBody>
        <ModalFooter>
            <Button Color="@AbortButtonColor" Clicked="AbortModal">@Localizer["Abort"]</Button>
            <Button Color="@ConfirmButtonColor" Clicked="SaveModal">@Localizer["Confirm"]</Button>
        </ModalFooter>
    </ModalContent>
</Modal>